from logger import logger
from config import config


RESTART_COUNT_PATH = "/mnt/usb/data/restart_count"


def increment_restart_count() -> int:
    # TODO: Task 12 — read, increment, write restart count file
    pass


def collect() -> dict:
    # TODO: Task 12 — collect CPU temp, memory, disk, reconnect count, last error, rows since sync
    pass
